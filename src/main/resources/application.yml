spring:
  application:
    name: Synamyk
  datasource:
    url: jdbc:postgresql://postgres.railway.internal:5432/railway
    username: postgres
    password: FbhZGZDFidcGZatlyOuPjUkKsXDfjvDG
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: false
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

server:
  port: 8080

finik:
  api-key: LOlnU70w7LtRpBZ5HAxLaVatjIXQLGS9xmeSmWM4
  account-id: d54479fa-bea8-4762-ba96-6ec8b1eaea26
  private-key-path: classpath:workkg.pem
  base-url: https://api.acquiring.averspay.kg
  webhook-url: ${APP_BASE_URL}/api/webhooks/finik

jwt:
  secret: ${JWT_SECRET:SynamykSuperSecretKeyForJwtTokenGenerationMinimum256BitsLong}
  expiration: ${JWT_EXPIRATION:86400000}
  refresh-expiration: ${JWT_REFRESH_EXPIRATION:2592000000}

sms:
  smspro:
    enabled: ${SMS_ENABLED:true}
    url: ${SMSPRO_URL:https://smspro.nikita.kg/api/message}
    login: ${SMSPRO_LOGIN:Cholpon123456}
    password: ${SMSPRO_PASSWORD:6EX5vJf8}
    sender: ${SMSPRO_SENDER:BilgirimKG}
    ttl-minutes: ${SMSPRO_TTL:5}

anthropic:
  api-key: ${ANTHROPIC_API_KEY}
  base-url: https://api.anthropic.com

minio:
  url: ${MINIO_URL:https://t3.storageapi.dev}
  access-key: ${MINIO_ACCESS_KEY:tid_aJGbHqsBDmeowWtqumTDArXwNunIgkRdQJNMSGJyxJqqPdVuHt}
  secret-key: ${MINIO_SECRET_KEY:tsec_9FiVEXo5kqDMJvu-FFX9BIDD-TALLDm5aaljUSmDTfngR22XIdfVmh2r7Ei--I6sLWq0Eb}
  bucket: ${MINIO_BUCKET:wrapped-basketcase-eqr42p}
  region: ${MINIO_REGION:auto}

admin:
  phone: ${ADMIN_PHONE:+996700000000}
  password: ${ADMIN_PASSWORD:Admin1234!}
  first-name: ${ADMIN_FIRST_NAME:Admin}

logging:
  level:
    synamyk: DEBUG
    org.springframework.security: WARN